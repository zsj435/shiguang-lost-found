# 校园失物招领平台

## 一、先看效果

双击本文件夹里的 **预览版.html**（推荐用 Chrome / Edge 打开）。

这是把整个前端打包成的**单个 HTML 文件**，不需要装 Node、不需要连数据库、不需要联网，
双击就能点。里面的数据是发布前抓的真实快照，页面可正常浏览、搜索、切换分类、
进详情页、点赞、发评论、登录（任意账号密码都能登，是离线模拟）。

注意：为了能离线运行，预览版的写操作（发布/点赞/评论）只改界面不落库，
刷新页面会还原；地图模式需要高德 key，预览版不提供。

## 二、后端服务（管理员 + 用户双角色）

> 原始工程（client / server 完整源码）不在本交付包中，现以 **server.js** 提供后端：
> 纯 Node 内置模块实现，**零 npm 依赖、不用装 MongoDB**，数据存 `data/db.json`，
> 首次启动自动写入演示账号与种子数据。删除 `data` 文件夹即可重置。

### 启动

- **方式 A**：双击 `start-server.bat`
- **方式 B**：命令行 `node server.js`（需 Node 18+，可用 `PORT` 环境变量改端口）

启动后有三个入口：

| 地址 | 是什么 |
|------|--------|
| http://localhost:3000/app | **终稿（推荐）**：离线预览版全量功能 × 落地页海报风 × 真实后端 |
| http://localhost:3000 | 离线预览版（打包快照，数据不落库） |
| http://localhost:3000/landing | 项目落地页 |

双击 `终稿.html` 用 file:// 打开也可以用（会自动连 http://localhost:3000 的接口）。

### 终稿功能一览（对齐离线预览版全量功能）

- **信息大厅**：列表 / 地图双模式（地图需配置高德 key，未配置时提示）、失物/招领/全部筛选、
  分类 chips（钥匙/电子产品/衣物/卡类证件/书籍/其他）、搜索、分页加载
- **登记处**：登录 / 注册（用户名 2-20 位可中文、密码 8-20 位含字母数字、学号选填→"已认证"标识）、演示账号一键填入
- **发布信息**：我丢了 / 我捡到了切换、图片上传（≤3 张，前端压缩，存 `data/uploads/`）、
  标题≤30 字、分类、发生时间、地点 + 详细位置、地图标记占位、详细描述≤500 字、
  联系方式（手机号 / 微信）；无图发布有确认弹窗；发布成功进入「正在匹配」相似推荐页
- **详情**：图片展示、浏览量、解决时间、联系方式卡（**未登录自动脱敏** 138\*\*\*\*1234，
  登录后可一键拨打 / 复制）、标记为已解决 / 撤销（作者或管理员）、编辑 / 删除、
  发起认领、认领核实、相似推荐（"这些招领可能是你丢的"）、评论与回复（删除自己的，管理员可删任意）
- **我的**：昵称 / 学号认证、发布统计（共发布 N 条 · 已解决 M）、我的发布管理、退出登录确认
- **消息通知**：评论 / 回复 / 点赞 / 认领 / 管理操作提醒，导航栏未读红点、全部已读
- **管理员台账**：统计面板、待处理认领核实、用户封禁 / 解封 / 授权 / 撤销 / 删除
- 双 token 自动续期：accessToken 2h 过期后前端拿 refreshToken 静默换新，无感知

### 账号

| 角色 | 账号 | 密码 | 能做什么 |
|------|------|------|----------|
| 管理员 | `admin` | `123456` | 用户管理（封禁/解封/授权管理员）、删除任意帖子评论、平台统计、处理认领、管理员代改帖子 |
| 普通用户 | `demo`（王同学） | `123456` | 发布/编辑/删除自己的帖子、评论、点赞、发起与处理认领 |
| 普通用户 | `li`（李同学） | `123456` | 同上 |
| 普通用户 | `zhao`（赵同学） | `123456` | 同上 |

注册的新账号一律是普通用户，管理员只能由已有管理员在后台授权。

### 角色差异速览

- **用户**：只能改/删自己的内容；被封禁后无法登录、无法写操作（code 40302）。
- **管理员**：`GET /api/admin/users`、`PATCH /api/admin/users/:id`、`DELETE /api/admin/users/:id`、
  `GET /api/admin/stats`、`GET /api/admin/claims` 等 `/api/admin/*` 接口仅管理员可访问（否则 code 40301）。

### 接口约定（与前端 request.js 对齐）

- 响应信封 `{ code, message, data, meta? }`，成功 `code=0`
- 双 token：`accessToken`（2h）+ `refreshToken`（7d）；过期返回 40102 触发前端静默刷新，无效返回 40101
- 主要接口：`/api/auth/*`（注册/登录/刷新/me）、`/api/posts`（列表/详情/发布/编辑/删除/点赞）、
  `/api/posts/:id/comments`、`/api/posts/:id/claims` + `/api/claims/:id/resolve`（认领流）、
  `/api/notifications`、`/api/admin/*`
- 完整自测：保持服务运行，执行 `node test-api.mjs`（32 项断言）

### 可配置项

- `PORT`：端口，默认 3000
- `JWT_SECRET`：token 签名密钥，生产环境务必替换（默认值仅供开发）

### 关于原来的 Express + MongoDB 工程

若拿到完整源码（`server/` + `client/`），仍可按方式 C 的流程运行；本目录的
`server.js` 与其接口约定一致，可作为轻量替代或联调用后端。

<details>
<summary>原方式 C：内存数据库快速预览（需要原始工程源码）</summary>

```bash
cd 项目源码/client && npm install && npm run build
cd ../server && npm install && node scripts/preview.js
```
</details>

## 三、目录结构（本交付包）

```
失物招领/
├── 终稿.html        最终交付：预览版全量功能 × 落地页海报风 × 真实后端
├── server.js        零依赖 Node 后端（双角色、双 token、上传、相似匹配、JSON 存储、静态托管）
├── start-server.bat 双击启动后端（Windows）
├── test-api.mjs     接口自测脚本（node test-api.mjs，38 项断言）
├── 落地页.html      项目宣传页（CTA 已深链到终稿）
├── 预览版.html      离线快照预览（不依赖后端）
└── 使用说明.md      本文件
```

## 四、主题

视觉主题叫「拾光」：主色拾光橙 #FF7A45（寻找/温度）+ 辅色安心绿 #12B886（已找到/招领）
+ 奶油纸底 #FDF7F1。语义双色是核心——寻物=橙、招领=绿，扫一眼颜色就知道状态。
要换肤只改 `client/src/styles/theme.css` 顶部的 CSS 变量即可。
